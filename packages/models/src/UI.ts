// import { types, model, Model, tProp, modelAction, idProp } from "mobx-keystone";

// @model("8HourRelay/UIState")
// export class UIState extends Model({
//   id: tProp(types.string),
//   locale: tProp(types.string).withSetter(),
//   theme: tProp(types.string).withSetter(),
//   path: tProp(types.string).withSetter(),
//   error: tProp(types.string).withSetter(),
//   source: tProp(types.string).withSetter(),
//   isLoading: tProp(types.boolean).withSetter(),
//   createdAt: tProp(types.number, () => new Date().getTime()),
//   updatedAt: tProp(types.number, () => new Date().getTime()).withSetter(),
// }) {}
